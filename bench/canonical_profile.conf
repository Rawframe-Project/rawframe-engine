# SPEC-0013's canonical overload profile (D212), which the bench serves the
# crowd under: at most two catch-up ticks an iteration, degraded (admission
# closed) 100 ms behind, overloaded at once 500 ms behind or after 5 s
# degraded.
world.maximum_ticks_per_iteration = 2
world.degraded_ms = 100
world.debt_limit_ms = 500
world.overload_ms = 5000
