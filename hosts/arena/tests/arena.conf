# Sixteen bots against the sample arena game for a second of play. The
# 64-bot measurement is the scenario test; this checks the host's wiring.
host.maximum_iterations = 120
host.iteration_rate = 120
world.tick_rate = 60
kest.game = games/arena/arena.game
network.loopback.latency_ms = 10
replication.endpoint = arena
bots.count = 16
bots.endpoint = arena
