# Three bots and room for two players: two play, and the third is told the
# server is full rather than left to time out.
host.maximum_iterations = 120
host.iteration_rate = 120
world.tick_rate = 60
kest.game = ../../../games/arena/arena.game
network.loopback.latency_ms = 10
replication.endpoint = arena
replication.maximum_connections = 2
bots.count = 3
bots.endpoint = arena
