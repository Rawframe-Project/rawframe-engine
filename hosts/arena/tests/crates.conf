# Sixteen bots against the sample physics game for a second of play; the
# server's physics summary shows the players' balls made and pushed.
host.maximum_iterations = 120
host.iteration_rate = 120
world.tick_rate = 60
kest.game = games/crates/crates.game
network.loopback.latency_ms = 10
replication.endpoint = arena
bots.count = 16
bots.endpoint = arena
