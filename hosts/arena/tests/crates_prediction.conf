# One bot predicting its ball in the sample physics game for five seconds:
# alone among the level, it mispredicts only while its input first arrives.
host.maximum_iterations = 600
host.iteration_rate = 120
world.tick_rate = 60
kest.game = ../../../games/crates/crates.game
network.loopback.latency_ms = 10
replication.endpoint = arena
bots.count = 1
bots.endpoint = arena
