# Two bots offer the ticket "pass" to a game whose own admission rule takes
# only "pass".
host.maximum_iterations = 120
host.iteration_rate = 120
world.tick_rate = 60
kest.game = ../../../modules/world_kest/tests/game/gate.game
network.loopback.latency_ms = 10
replication.endpoint = arena
bots.count = 2
bots.endpoint = arena
bots.ticket = pass
