# SPEC-0041's divergence drill (D204): four bots predict their runners and
# flip one bit of every state they hash, so the server finds every checksum
# record diverged and logs each, and plays on. Development builds only.
host.maximum_iterations = 600
host.iteration_rate = 120
world.tick_rate = 60
kest.game = ../../../games/runners/runners.game
network.loopback.latency_ms = 10
replication.endpoint = arena
bots.count = 4
bots.endpoint = arena
bots.checksum_interval = 30
bots.divergence_drill = true
