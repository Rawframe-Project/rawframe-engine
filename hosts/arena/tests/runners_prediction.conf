# Four bots predicting their runners in the sample platformer for five
# seconds: characters among a static level mispredict only while each bot's
# input first arrives.
host.maximum_iterations = 600
host.iteration_rate = 120
world.tick_rate = 60
kest.game = games/runners/runners.game
kest.library = third_party/kest/lib/
network.loopback.latency_ms = 10
replication.endpoint = arena
bots.count = 4
bots.endpoint = arena
