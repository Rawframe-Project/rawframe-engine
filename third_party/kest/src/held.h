#ifndef KEST_HELD_H
#define KEST_HELD_H

// A machine a host keeps across frames and reloads under a world, made of the
// public doors and nothing else. What it declares is in `kest.h`, because a
// host is what calls it. See D1151.
#include "kest.h"

#endif
