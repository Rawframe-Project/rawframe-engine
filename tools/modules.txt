# The module graph (ADR-0002). One line per module: `name: allowed dependencies`.
# The build refuses a dependency that is not on the module's line, and
# tools/check_repo.py refuses an #include of another module's headers that is
# not on it either. Keep the graph a DAG with base as its only leaf.

base:
result: base
diagnostics: result base
execution: diagnostics result base
composition: execution diagnostics result base
schema: result base
world: schema execution diagnostics result base
kest: execution result base
network: execution result base
network_loopback: network execution result base
world_runtime: world composition schema execution diagnostics result base
world_kest: world_replication world_runtime kest network world composition schema execution diagnostics result base
world_replication: world_runtime network world composition schema execution diagnostics result base
host: composition execution diagnostics result base
dedicated_server: host world_kest world_runtime composition diagnostics result base
