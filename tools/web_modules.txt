# Modules that build for the web (ADR-0084): wasm32-wasi without threads, the
# `wasm-development` preset. Each module's dependencies on its
# tools/modules.txt line are here too (tools/check_repo.py). Every module a
# client needs joins this list before the web client exists.
base result diagnostics execution composition document schema world world_snapshot world_save world_runtime scene authoring animation localization mesh input
